__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/

# Blender
*.blend1
*.blend2

# Export output. `data/` is generated by the exporter and consumed by the runtime; authored
# source (meshes, textures, UI) lives in the project's own source folders, never here.
/data/

# .NET (tools/ParadiseBlenderBridge)
bin/
obj/

.DS_Store
